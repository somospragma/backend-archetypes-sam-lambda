{
  "name": "backend-archetypes-sam-lambda",
  "version": "1.0.0",
  "description": "aplicacion de serverless con dynamoDb y lambda AWS V3",
  "main": "app.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "jest --watchAll --verbose --coverage" 
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.506.0",
    "@aws-sdk/lib-dynamodb": "^3.506.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
